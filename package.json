{
	"type": "module",
	"name": "@oh-my-pi/animations",
	"version": "0.1.0",
	"private": true,
	"description": "Ambient animated TUI widgets for omp, as a single config-driven package.",
	"license": "MIT",
	"author": "Rohit Gudi",
	"homepage": "https://github.com/caretak3r/omp-animations#readme",
	"bugs": "https://github.com/caretak3r/omp-animations/issues",
	"repository": {
		"type": "git",
		"url": "git+https://github.com/caretak3r/omp-animations.git"
	},
	"keywords": [
		"tui",
		"animation",
		"terminal",
		"extension",
		"plugin",
		"cli",
		"oh-my-pi"
	],
	"scripts": {
		"check": "biome check . && bun run check:types",
		"check:types": "tsgo -p tsconfig.json --noEmit",
		"test": "bun test",
		"lint": "biome lint .",
		"fix": "biome check --write --unsafe .",
		"fmt": "biome format --write .",
		"probe": "bun scripts/frame-probe.ts",
		"probe:lint": "bun scripts/frame-lint.ts"
	},
	"dependencies": {
		"@oh-my-pi/pi-tui": "^17",
		"@oh-my-pi/pi-utils": "^17"
	},
	"peerDependencies": {
		"@oh-my-pi/pi-coding-agent": "^17"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.16",
		"@oh-my-pi/pi-coding-agent": "17.3.4",
		"@types/bun": "^1.3.14",
		"@typescript/native-preview": "7.0.0-dev.20260609.1"
	},
	"engines": {
		"bun": ">=1.3.14"
	},
	"files": [
		"src",
		"README.md",
		"CHANGELOG.md",
		"LICENSE"
	],
	"omp": {
		"name": "oh-my-pi animations",
		"description": "Ambient animated TUI widgets for omp, as a single config-driven package.",
		"extensions": [
			"./src/registrar.ts"
		],
		"settings": {
			"animations": {
				"type": "enum",
				"description": "Shared motion tier for all animations (each still gates to 'off' on non-TTY/CI/NO_COLOR). Default 'subtle' is the curated native posture.",
				"values": [
					"off",
					"subtle",
					"full"
				],
				"default": "subtle",
				"env": "OMP_ANIMATIONS"
			},
			"animationsBoxDetail": {
				"type": "enum",
				"description": "How much the Animations Box shows per enabled segment. 'readable' (default) gives one labeled row per required segment and keeps Agent Bonsai to one line per agent. 'detailed' adds Agent Bonsai's activity/provenance chains. 'simple' composes everything into a single line. Applied at wire time (restart to change).",
				"values": [
					"simple",
					"readable",
					"detailed"
				],
				"default": "readable",
				"env": "OMP_ANIMATIONS_BOX_DETAIL"
			},
			"animationsBoxPlacement": {
				"type": "enum",
				"description": "Which side of the editor the Animations Box mounts on. Applied at wire time (restart to change).",
				"values": [
					"aboveEditor",
					"belowEditor"
				],
				"default": "belowEditor",
				"env": "OMP_ANIMATIONS_BOX_PLACEMENT"
			},
			"animationsContextQuota": {
				"type": "number",
				"description": "Context-window budget the Audit Box's top row fills against, as a percentage of the model's window — roughly where compaction lands. The fill visual and its turn forecast count down to this ceiling, not to 100%.",
				"default": 80,
				"min": 5,
				"max": 100,
				"env": "OMP_ANIMATIONS_CONTEXT_QUOTA"
			},
			"agentBonsai": {
				"type": "boolean",
				"description": "Show Agent Bonsai inside the Audit Box when a subagent exists.",
				"default": true,
				"env": "OMP_ANIMATIONS_AGENT_BONSAI"
			},
			"agentRosterDetail": {
				"type": "string",
				"description": "Select compact or verbose activity text for exact agent rows.",
				"default": "compact",
				"enum": [
					"compact",
					"verbose"
				],
				"env": "OMP_ANIMATIONS_AGENT_ROSTER_DETAIL"
			},
			"animationsBonsaiSettleSeconds": {
				"type": "number",
				"description": "Keep completed agents for this many seconds, with an 800ms announcement minimum in every motion tier.",
				"default": 300,
				"min": 0,
				"max": 86400,
				"env": "OMP_ANIMATIONS_BONSAI_SETTLE_SECONDS"
			},
			"liveFiles": {
				"type": "boolean",
				"description": "Show paths currently being edited or written in the Audit Box.",
				"default": true,
				"env": "OMP_ANIMATIONS_LIVE_FILES"
			},
			"recurrenceStrip": {
				"type": "boolean",
				"description": "Show whether recent turns are making heading progress or repeating a tool orbit.",
				"default": true,
				"env": "OMP_ANIMATIONS_RECURRENCE_STRIP"
			},
			"contextRewriteShadow": {
				"type": "boolean",
				"description": "Show context rewrite metrics when the host stripped ≥512 tokens. `sent` is provider-reported usage; `shown` is measured or estimated.",
				"default": true,
				"env": "OMP_ANIMATIONS_CONTEXT_REWRITE_SHADOW"
			},
			"compactionScar": {
				"type": "boolean",
				"description": "Show tokens cut by compaction and immediate file re-reads.",
				"default": true,
				"env": "OMP_ANIMATIONS_COMPACTION_SCAR"
			},
			"consentLock": {
				"type": "boolean",
				"description": "Show pending tool approvals as a blocking consent signal.",
				"default": true,
				"env": "OMP_ANIMATIONS_CONSENT_LOCK"
			},
			"sessionPhylogeny": {
				"type": "boolean",
				"description": "Show the active session node, branch depth, and sibling count after tree navigation.",
				"default": true,
				"env": "OMP_ANIMATIONS_SESSION_PHYLOGENY"
			},
			"thinkActLissajous": {
				"type": "boolean",
				"description": "Show the latest assistant response's thinking-to-action balance.",
				"default": false,
				"env": "OMP_ANIMATIONS_THINK_ACT_LISSAJOUS"
			},
			"errorIsotope": {
				"type": "boolean",
				"description": "Show a normalized tool error only when the same signature repeats.",
				"default": true,
				"env": "OMP_ANIMATIONS_ERROR_ISOTOPE"
			},
			"skillChromatograph": {
				"type": "boolean",
				"description": "Show skills actually used during the current turn.",
				"default": true,
				"env": "OMP_ANIMATIONS_SKILL_CHROMATOGRAPH"
			},
			"retryRadar": {
				"type": "boolean",
				"description": "Show active automatic retry and fallback progress.",
				"default": true,
				"env": "OMP_ANIMATIONS_RETRY_RADAR"
			},
			"verify": {
				"type": "boolean",
				"description": "Show how many writes have landed since the last successful bash.",
				"default": true,
				"env": "OMP_ANIMATIONS_VERIFY"
			},
			"authBeacon": {
				"type": "boolean",
				"description": "Show a persistent alert when a provider credential is auto-disabled.",
				"default": true,
				"env": "OMP_ANIMATIONS_AUTH_BEACON"
			},
			"asyncJobHarbor": {
				"type": "boolean",
				"description": "Show running, ready, failed, and cancelled async job (background bash/task) counts.",
				"default": true,
				"env": "OMP_ANIMATIONS_ASYNC_JOB_HARBOR"
			},
			"goalHeading": {
				"type": "boolean",
				"description": "Show the active goal objective, status, and token-budget heading.",
				"default": false,
				"env": "OMP_ANIMATIONS_GOAL_HEADING"
			},
			"ttftSplit": {
				"type": "boolean",
				"description": "Show turn-start to first-assistant-message latency.",
				"default": true,
				"env": "OMP_ANIMATIONS_TTFT_SPLIT"
			},
			"memoryBackendTide": {
				"type": "boolean",
				"description": "Show memory backend state, observed count deltas, and reported recall activity.",
				"default": true,
				"env": "OMP_ANIMATIONS_MEMORY_BACKEND_TIDE"
			},
			"darkroomTitle": {
				"type": "boolean",
				"description": "Project critical animation state into the terminal title.",
				"default": false,
				"env": "OMP_ANIMATIONS_DARKROOM_TITLE"
			},
			"auditTrailBoxPlacement": {
				"type": "enum",
				"description": "Which side of the editor Audit Trail Box mounts on. Applied at wire time (restart to change).",
				"values": [
					"aboveEditor",
					"belowEditor"
				],
				"default": "belowEditor",
				"env": "OMP_ANIMATIONS_AUDIT_TRAIL_BOX_PLACEMENT"
			},
			"auditTrailBoxAccentColor": {
				"type": "enum",
				"description": "Audit Trail Box's primary accent — the box badge (the per-status risk ramp stays fixed). 'default' keeps the built-in palette. Applied at wire time (restart to change).",
				"values": [
					"default",
					"accent",
					"success",
					"warning",
					"error",
					"muted",
					"syntaxKeyword",
					"syntaxFunction",
					"syntaxVariable",
					"syntaxType",
					"syntaxString",
					"syntaxNumber",
					"okabeOrange",
					"okabeSkyBlue",
					"okabeGreen",
					"okabeYellow",
					"okabeBlue",
					"okabeVermillion",
					"okabePurple"
				],
				"default": "default",
				"env": "OMP_ANIMATIONS_AUDIT_TRAIL_BOX_ACCENT_COLOR"
			},
			"breathingBorder": {
				"type": "boolean",
				"description": "Breathe the Audit Box border instead of holding it steady.",
				"default": true,
				"env": "OMP_ANIMATIONS_BREATHING_BORDER"
			},
			"breathingBorderPlacement": {
				"type": "enum",
				"description": "Which side of the editor Breathing Border mounts on. Applied at wire time (restart to change).",
				"values": [
					"aboveEditor",
					"belowEditor"
				],
				"default": "aboveEditor",
				"env": "OMP_ANIMATIONS_BREATHING_BORDER_PLACEMENT"
			},
			"breathingBorderAccentColor": {
				"type": "enum",
				"description": "Breathing Border's primary accent — the border's peak brightness (the resting/mid-brightness tokens stay fixed). 'default' keeps the built-in palette. Applied at wire time (restart to change).",
				"values": [
					"default",
					"accent",
					"success",
					"warning",
					"error",
					"muted",
					"syntaxKeyword",
					"syntaxFunction",
					"syntaxVariable",
					"syntaxType",
					"syntaxString",
					"syntaxNumber",
					"okabeOrange",
					"okabeSkyBlue",
					"okabeGreen",
					"okabeYellow",
					"okabeBlue",
					"okabeVermillion",
					"okabePurple"
				],
				"default": "default",
				"env": "OMP_ANIMATIONS_BREATHING_BORDER_ACCENT_COLOR"
			},
			"cacheMeterPlacement": {
				"type": "enum",
				"description": "Which side of the editor Cache Meter mounts on. Applied at wire time (restart to change).",
				"values": [
					"aboveEditor",
					"belowEditor"
				],
				"default": "aboveEditor",
				"env": "OMP_ANIMATIONS_CACHE_METER_PLACEMENT"
			},
			"cacheMeterAccentColor": {
				"type": "enum",
				"description": "Cache Meter's primary accent — the badge color (the invalidation alert stays fixed error-red). 'default' keeps the built-in palette. Applied at wire time (restart to change).",
				"values": [
					"default",
					"accent",
					"success",
					"warning",
					"error",
					"muted",
					"syntaxKeyword",
					"syntaxFunction",
					"syntaxVariable",
					"syntaxType",
					"syntaxString",
					"syntaxNumber",
					"okabeOrange",
					"okabeSkyBlue",
					"okabeGreen",
					"okabeYellow",
					"okabeBlue",
					"okabeVermillion",
					"okabePurple"
				],
				"default": "default",
				"env": "OMP_ANIMATIONS_CACHE_METER_ACCENT_COLOR"
			},
			"rateLimitTidepoolPlacement": {
				"type": "enum",
				"description": "Which side of the editor Rate-Limit Tidepool mounts on. Applied at wire time (restart to change).",
				"values": [
					"aboveEditor",
					"belowEditor"
				],
				"default": "belowEditor",
				"env": "OMP_ANIMATIONS_RATE_LIMIT_TIDEPOOL_PLACEMENT"
			},
			"rateLimitTidepoolAccentColor": {
				"type": "enum",
				"description": "Rate-Limit Tidepool's primary accent — the water color (the near-empty sand alarm stays fixed warning-amber). 'default' keeps the built-in palette. Applied at wire time (restart to change).",
				"values": [
					"default",
					"accent",
					"success",
					"warning",
					"error",
					"muted",
					"syntaxKeyword",
					"syntaxFunction",
					"syntaxVariable",
					"syntaxType",
					"syntaxString",
					"syntaxNumber",
					"okabeOrange",
					"okabeSkyBlue",
					"okabeGreen",
					"okabeYellow",
					"okabeBlue",
					"okabeVermillion",
					"okabePurple"
				],
				"default": "default",
				"env": "OMP_ANIMATIONS_RATE_LIMIT_TIDEPOOL_ACCENT_COLOR"
			}
		}
	}
}
